{
  "name": "geovera-staging",
  "version": "1.0.0",
  "description": "GeoVera Brand Intelligence Platform",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed'",
    "generate": "node generate-report-local.js",
    "serve": "./serve-local.sh"
  },
  "dependencies": {
    "@vercel/node": "^3.2.29"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
